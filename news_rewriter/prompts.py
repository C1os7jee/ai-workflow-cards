"""各步骤的 prompt 模板。改 prompt 只动这里。"""
from style_card import STYLE_CARD


FACT_ANCHOR_PROMPT = """你是事实抽取助手。读下面这条资讯,抽出所有**不可改动的硬事实**。

资讯原文:
\"\"\"
{content}
\"\"\"

抽取要求:
- tools: 资讯中提到的工具/产品/服务名 (完整名称)
- numbers: 资讯中的数字 (带单位+语境,例如 "节省时间95%(原文宣称)")
- features: 工具/方法的具体功能
- people_orgs: 出现的人物或机构
- claims: 资讯里的明确宣称 (用"原文宣称:"开头)
- raw_text_summary: 一句话说清这条资讯讲了什么 (≤40字)

注意:
- 只抽原文里**真实出现**的东西,不要推理、不要补全
- 数字一定要带单位和语境,不能光给数字
- 区分"客观事实"和"原文宣称"

只输出 JSON,不要其他文字:
{{
  "tools": [...],
  "numbers": [...],
  "features": [...],
  "people_orgs": [...],
  "claims": [...],
  "raw_text_summary": "..."
}}
"""


SELECTION_PROMPT = """你是选题判断助手。下面这个IP的定位是:
{style_card}

---

现在有一条资讯,事实摘要是:
{fact_summary}

判断这条资讯是否适合按这个IP的角度改写。
给出建议即可,不阻塞流程。

考虑维度:
- 是否符合"一个人省重复劳动"的选题方向
- 受众(职场人/程序员/产品经理/自由职业者)是否关心
- 改写后是否有具体可用的东西能给到读者
- 改写后可能的风险(如:工具尚未发布/事实模糊/容易写成营销)

只输出 JSON:
{{
  "worth_writing": true/false,
  "angle": "建议的切入角度,或者'不建议'的理由 (≤50字)",
  "risk_level": "low/medium/high",
  "note": "其他提醒,可选"
}}
"""


REWRITE_PROMPT = """你是按特定IP风格改写资讯的助手。

# 风格卡
{style_card}

---

# 原始资讯
{original_content}

# 锚定事实(改写时不能改这些)
{fact_anchors}

# 选题建议
{recommendation}

---

# 你的任务
按风格卡改写这条资讯,产出完整内容包。

要求:
1. 标题: 生成 3-5 个候选,每个都要满足"我+动作+结果+数字+反差/悬念"
2. 开头: 生成 2-3 个开头候选,切入角度不同(画面/结果/反差)
3. 正文: 完整改写,严格按风格卡的结构和句式
4. 结尾: 资料型/追更型/提问型/悬念型 各给一个版本

# 关于"似真例子"
凡需要"我"的真实使用经历/具体场景/踩坑/截图的地方:
- 基于原文事实合理展开,生成一个"似真例子"
- 整段用 [似真例子,待核对]: 开头,以 [/似真例子] 结尾
- 例: [似真例子,待核对]: 我拿上周开会的录音试了下,1小时的会议2分钟出纪要,但有3个人名识别错了。[/似真例子]
- 严禁: 编造原文里没有的工具/数字/功能;写"我实测""我用了一周"这种无法验证的实证

# 输出格式
只输出 JSON:
{{
  "title_candidates": ["...", "...", "..."],
  "opening_candidates": ["...", "...", "..."],
  "body": "完整正文,含[似真例子]标记",
  "ending_options": {{
    "资料型": "...",
    "追更型": "...",
    "提问型": "...",
    "悬念型": "..."
  }}
}}
"""


def build_style_card() -> str:
    return STYLE_CARD